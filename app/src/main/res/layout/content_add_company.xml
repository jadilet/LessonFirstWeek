<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:showIn="@layout/activity_add_company"
    tools:context="com.developer.artisla.lessonfirstweek.AddCompany">

    <EditText
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:hint="Company Name"
        android:id="@+id/titleEdt"
        />
    <EditText
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/titleEdt"
        android:hint="Description"
        android:id="@+id/descriptionEdt"
        />
    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/descriptionEdt"
        android:text="Add Logo"
        android:id="@+id/addLogoBtn"
        />
    <ImageView
        android:layout_width="fill_parent"
        android:layout_height="500px"
        android:layout_below="@id/addLogoBtn"
        android:id="@+id/logoImv"
        />
    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/logoImv"
        android:text="Add Company"
        android:id="@+id/addCompanyBtn"
        />

</RelativeLayout>
